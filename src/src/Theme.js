const primaryColor = '#64b5f6';

export default {
    primaryColor,
    type: 'light',
    saveToolbar: {
        background: primaryColor,
        button: {
            borderRadius: 3,
            height: 32
        }
    }
}